SELECT
    da.host_name AS asset_name,
    da.ip_address,
    COUNT(DISTINCT fas.scan_id) AS scans_completed,
    string_agg(DISTINCT to_char(ds.finished, 'YYYY-MM-DD'), ', ' ORDER BY to_char(ds.finished, 'YYYY-
